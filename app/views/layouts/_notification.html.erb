<% flash.each do |type, msg| %>
  <div class="mt-3">
    <%= "<div class=\"alert alert-danger mb-1\" >#{msg}</div>".html_safe if type == 'alert' %>
    <%= "<div class=\"alert alert-success mb-1\" >#{msg}</div>".html_safe if type == 'notice' %>
  </div>
<% end %>