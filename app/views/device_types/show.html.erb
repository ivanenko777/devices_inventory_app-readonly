<% content_for :page_head do %>
  <h1 class="h2">Device type #<%= @device_type.id %></h1>
<% end %>

<%= link_to 'Back', device_types_path %>
| <%= link_to 'Edit', edit_device_type_path(@device_type) %>
| <%= link_to 'Delete', @device_type, method: :delete, data: {confirm: 'Are you sure?'} %>

<p>
  <strong>Sys name:</strong>
  <%= @device_type.sys_name %>
</p>

<p>
  <strong>Name:</strong>
  <%= @device_type.name %>
</p>